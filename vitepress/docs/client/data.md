# 数据备份/恢复

添加服务器时，将服务器设置为主服务器，可以启用数据备份功能。设置后，会将服务器信息自动备份到主服务器，以防止数据丢失。
![](../screenshot/master_server.png)


::: tip
恢复数据时，只需将主服务器的 API Url 和 Token 填入新客户端，并设置为主服务器，即可自动恢复备份的数据。
:::

也可以在`设置`页面，单独配置`API Url` 和 `Token` 来恢复数据。
![](../screenshot/setting.png)
